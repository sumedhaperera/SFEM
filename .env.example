# --- Salesforce (MVP uses username+password+token) ---
SF_USERNAME=your.username@example.com
SF_PASSWORD=yourSalesforcePassword
SF_SECURITY_TOKEN=yourSecurityToken
SF_DOMAIN=login  # or 'test' for sandboxes

# --- LLM Provider (OpenAI-style API) ---
OPENAI_API_KEY=          # add your key here (leave blank if not testing LLM)
OPENAI_MODEL=gpt-4o-mini # or similar chat model

# --- Local paths / settings ---
DATA_DIR=./data
INDEX_DIR=./index
TOP_K=4
TOKENIZERS_PARALLELISM=false

# --- Retrieval backend toggle ---
VECTOR_BACKEND=qdrant    # faiss | qdrant

# --- Qdrant (used when VECTOR_BACKEND=qdrant) ---
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=          # leave blank for local; set for Qdrant Cloud
QDRANT_COLLECTION=sfem_kb
QDRANT_DISTANCE=Cosine   # Cosine | Dot | Euclid (must match how you embed)

# --- Database (Postgres) ---
# Heroku sets DATABASE_URL automatically. For local dev, use Docker quickstart:
#   docker run --name sfem-pg -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=sfem -p 5432:5432 -d postgres:16
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sfem

# --- Embedding model (keep dimension consistent across FAISS/Qdrant) ---
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2

